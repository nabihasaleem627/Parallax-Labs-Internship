/**
 * Shared domain types, re-exported for convenience.
 * Keep this surface small — types are defined next to their implementation.
 */
export type { SessionClaims } from "@/lib/auth/jwt";
export type { CurrentUser } from "@/lib/auth/session";
export type { FieldErrors } from "@/lib/api";
